import React from "react";
import Workflow from "../../components/workflow/Workflow";

const Workflow_Section = () => {
  return (
    <div>
      <Workflow />
    </div>
  );
};

export default Workflow_Section;
